

# API

Anything in the `/api` folder corresponds to the UMG api that is used by mods.

We use the `ModLoader` to load the API.

---

- `server/api`: Server-only API
- `client/api`: Client-only API
- `common/api`: Shared API



