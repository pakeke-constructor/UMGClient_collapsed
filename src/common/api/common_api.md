
# common api

Api tables that are shared between client and server.

common API is responsible for:
`math`
`util`
`timer`
`physics`
`event` (`on` and `call`)

And a few other helper functions.
See `util.lua`

